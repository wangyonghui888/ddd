<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.panda.sport.rcs.mapper.RcsQuotaUserSingleSiteQuotaMapper">
    <select id="selectRcsQuotaUserSingleSiteQuota" resultType="com.panda.sport.rcs.pojo.RcsQuotaUserSingleSiteQuota">
        SELECT * FROM rcs_quota_user_single_site_quota WHERE sport_id=#{sportId} AND `status`=1 ORDER BY template_level
    </select>
</mapper>